/**
 * @description Action constants to be used for loader and its feature
 *
 * format: {feature}_{action}
 */
const loaderActions = {
    SHOW_LOADER: 'SHOW_LOADER',
    HIDE_LOADER: 'HIDE_LOADER'
  };
  
  export default loaderActions;
  